"""Data scanning utilities."""

import pandas as pd
from loguru import logger


def scan_data(data: pd.DataFrame) -> pd.DataFrame:
    """Scan data for quality issues."""
    logger.info("Data scanning not yet implemented")
    return pd.DataFrame()
